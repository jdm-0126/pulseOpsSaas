{
  "name": "@pulseops/worker",
  "version": "0.0.1",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/worker.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "bullmq": "^5.0.0",
    "@pulseops/logger": "*",
    "@pulseops/db": "*",
    "@pulseops/types": "*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0"
  }
}
